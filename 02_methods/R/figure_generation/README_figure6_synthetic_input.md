# Figure 6 synthetic input note

`figure6_variance_disentanglement.R` requires the precomputed synthetic benchmark table:

04_results/synthetic_validation_v2/tables/synthetic_scenario_summary.tsv

This table was not included in the current lightweight real-data runner. Figure 6 is therefore treated as an optional synthetic-validation module and is not executed by `run_nm2_pipeline.R`.

The script is retained for transparency and can be executed after regenerating or providing the required synthetic benchmark table.
